---
import Layout from '../../layouts/Layout.astro';
import { courses } from '../../data/courses';

const c4Course = courses.find(c => c.slug === 'c4');
if (!c4Course) {
  throw new Error("No se encontró el curso 'c4' en el archivo de datos.");
}
---

<Layout title="Modelo C4: Arquitectura Visual" description="Aprende a mapear la arquitectura de tu software en diferentes niveles de abstracción (Contexto, Contenedores, Componentes, Código)." courseSlug="c4">
  <div class="course-container">
    <header class="course-index-header">
      <h1 class="course-big-title" style={`--course-color: ${c4Course.color}`}>
         🗺️ {c4Course.name}
      </h1>
      <p class="course-description">
         <strong>"El Google Maps de tu Software"</strong> — {c4Course.description}
      </p>

      <div class="tools-links">
        <span class="tools-label">📜 Herramientas del Arquitecto:</span>
        <div class="tools-grid">
          <a href="https://c4model.com/" target="_blank" rel="noopener">🌐 C4 Model Oficial</a>
          <a href="https://structurizr.com/" target="_blank" rel="noopener">🏗️ Structurizr</a>
          <a href="https://www.drawio.com/" target="_blank" rel="noopener">𓁑 Draw.io</a>
        </div>
      </div>
    </header>

    <div class="cards-grid">
      {c4Course.lessons?.map((lesson, index) => (
        <a href={lesson.href} class="lesson-card">
          <div class="lesson-card__number">{index + 1}</div>
          <div class="lesson-card__content">
            <h3 class="lesson-card__title">🧭 {lesson.title}</h3>
            <span class="lesson-card__cta">Empezar nivel →</span>
          </div>
        </a>
      ))}
    </div>
  </div>
</Layout>

<style>
  .course-container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }

  .course-index-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .course-big-title {
    font-size: clamp(2rem, 7vw, 3.5rem);
    font-weight: 900;
    color: var(--course-color, #1a5ebd);
    margin-bottom: 1rem;
    line-height: 1;
    font-family: 'Nunito', sans-serif;
    text-shadow: 4px 4px 0px var(--border);
  }

  .course-description {
    font-size: 1.25rem;
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto;
    font-weight: 700;
  }

  .tools-links {
    margin-top: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
  }

  .tools-label {
    font-family: 'JetBrains Mono', monospace;
    font-weight: 800;
    font-size: 0.9rem;
    color: #475569;
  }

  .tools-grid {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  .tools-grid a {
    background: white;
    border: 2px solid var(--border);
    padding: 0.4rem 0.8rem;
    border-radius: 8px;
    text-decoration: none;
    color: #1a1a1b;
    font-weight: 900;
    font-size: 0.95rem;
    box-shadow: 3px 3px 0px var(--border);
    transition: transform 0.1s, box-shadow 0.1s;
  }

  .tools-grid a:hover {
    transform: translate(-1px, -1px);
    box-shadow: 4px 4px 0px var(--border);
    border-color: #1a5ebd;
  }

  .cards-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1.5rem;
  }

  .lesson-card {
    display: flex;
    gap: 1rem;
    background: white;
    border: 3px solid var(--border);
    border-radius: 20px;
    padding: 1.25rem;
    text-decoration: none;
    color: var(--text-primary);
    transition: transform 0.2s, box-shadow 0.2s;
    box-shadow: 4px 4px 0px var(--border);
    align-items: center;
  }

  .lesson-card:hover {
    transform: translate(-4px, -4px);
    box-shadow: 8px 8px 0px var(--border);
  }

  .lesson-card__number {
    font-size: 2rem;
    font-weight: 900;
    color: #e2e8f0;
    line-height: 1;
  }

  .lesson-card__title {
    font-size: 1.15rem;
    font-weight: 900;
    margin: 0 0 0.25rem;
  }

  .lesson-card__cta {
    font-size: 0.9rem;
    font-weight: 800;
    color: #1a5ebd;
  }
</style>
