export interface GameDungeonSolution {
  python: string;
  java: string;
  csharp: string;
  cpp: string;
  javascript: string;
  php: string;
  pseint: string;
}

export const gameDungeonSolutions: Record<number, GameDungeonSolution> = {
  // 1. Mostrar Mapa de la Mazmorra
  1: {
    python: `def mostrarMapaMazmorra(matriz):\n    print("🗺️ === MAPA DE LA MAZMORRA ===")\n    for fila in matriz:\n        print(" ".join(fila))\n\nmapa = [\n    ["#", "#", "#", "#", "#"],\n    ["#", "P", ".", "C", "#"],\n    ["#", ".", "#", ".", "#"],\n    ["#", "E", ".", "X", "#"],\n    ["#", "#", "#", "#", "#"]\n]\nmostrarMapaMazmorra(mapa)`,
    java: `public class Main {\n    public static void mostrarMapaMazmorra(char[][] matriz) {\n        System.out.println("🗺️ === MAPA DE LA MAZMORRA ===");\n        for (int r = 0; r < matriz.length; r++) {\n            for (int c = 0; c < matriz[r].length; c++) {\n                System.out.print(matriz[r][c] + " ");\n            }\n            System.out.println();\n        }\n    }\n    public static void main(String[] args) {\n        char[][] mapa = {\n            {'#', '#', '#', '#', '#'},\n            {'#', 'P', '.', 'C', '#'},\n            {'#', '.', '#', '.', '#'},\n            {'#', 'E', '.', 'X', '#'},\n            {'#', '#', '#', '#', '#'}\n        };\n        mostrarMapaMazmorra(mapa);\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static void MostrarMapaMazmorra(char[,] matriz) {\n        Console.WriteLine("🗺️ === MAPA DE LA MAZMORRA ===");\n        for (int r = 0; r < matriz.GetLength(0); r++) {\n            for (int c = 0; c < matriz.GetLength(1); c++) {\n                Console.Write(matriz[r, c] + " ");\n            }\n            Console.WriteLine();\n        }\n    }\n    static void Main() {\n        char[,] mapa = {\n            {'#', '#', '#', '#', '#'},\n            {'#', 'P', '.', 'C', '#'},\n            {'#', '.', '#', '.', '#'},\n            {'#', 'E', '.', 'X', '#'},\n            {'#', '#', '#', '#', '#'}\n        };\n        MostrarMapaMazmorra(mapa);\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\nvoid mostrarMapaMazmorra(const std::vector<std::vector<char>>& matriz) {\n    std::cout << "🗺️ === MAPA DE LA MAZMORRA ===\\n";\n    for (const auto& fila : matriz) {\n        for (char c : fila) std::cout << c << " ";\n        std::cout << "\\n";\n    }\n}\nint main() {\n    std::vector<std::vector<char>> mapa = {\n        {'#', '#', '#', '#', '#'},\n        {'#', 'P', '.', 'C', '#'},\n        {'#', '.', '#', '.', '#'},\n        {'#', 'E', '.', 'X', '#'},\n        {'#', '#', '#', '#', '#'}\n    };\n    mostrarMapaMazmorra(mapa);\n    return 0;\n}`,
    javascript: `function mostrarMapaMazmorra(matriz) {\n    console.log("🗺️ === MAPA DE LA MAZMORRA ===");\n    matriz.forEach(fila => console.log(fila.join(" ")));\n}\nconst mapa = [\n    ["#", "#", "#", "#", "#"],\n    ["#", "P", ".", "C", "#"],\n    ["#", ".", "#", ".", "#"],\n    ["#", "E", ".", "X", "#"],\n    ["#", "#", "#", "#", "#"]\n];\nmostrarMapaMazmorra(mapa);`,
    php: `<?php\nfunction mostrarMapaMazmorra($matriz) {\n    echo "🗺️ === MAPA DE LA MAZMORRA ===\\n";\n    foreach ($matriz as $fila) {\n        echo implode(" ", $fila) . "\\n";\n    }\n}\n$mapa = [\n    ["#", "#", "#", "#", "#"],\n    ["#", "P", ".", "C", "#"],\n    ["#", ".", "#", ".", "#"],\n    ["#", "E", ".", "X", "#"],\n    ["#", "#", "#", "#", "#"]\n];\nmostrarMapaMazmorra($mapa);\n?>`,
    pseint: `SubProceso mostrarMapaMazmorra(mapa)\n    Escribir "🗺️ === MAPA DE LA MAZMORRA ==="\n    Escribir "# # # # #"\n    Escribir "# P . C #"\n    Escribir "# . # . #"\n    Escribir "# E . X #"\n    Escribir "# # # # #"\nFinSubProceso\n\nAlgoritmo Principal\n    mostrarMapaMazmorra("")\nFinAlgoritmo`
  },

  // 2. Localizar Coordenadas del Jugador ('P')
  2: {
    python: `def localizarJugador(mapa):\n    for f, fila in enumerate(mapa):\n        for c, celda in enumerate(fila):\n            if celda == "P":\n                return f"📍 Héroe 'P' localizado en Fila {f}, Columna {c} (Coords: [{f},{c}])."\n    return "❌ Jugador no encontrado en el mapa."\n\nmapa = [\n    ["#", "#", "#", "#"],\n    ["#", ".", "P", "#"],\n    ["#", "X", ".", "#"],\n    ["#", "#", "#", "#"]\n]\nprint(localizarJugador(mapa))`,
    java: `public class Main {\n    public static String localizarJugador(char[][] mapa) {\n        for (int r = 0; r < mapa.length; r++) {\n            for (int c = 0; c < mapa[r].length; c++) {\n                if (mapa[r][c] == 'P') {\n                    return "📍 Héroe 'P' localizado en Fila " + r + ", Columna " + c + ".";\n                }\n            }\n        }\n        return "❌ No encontrado.";\n    }\n    public static void main(String[] args) {\n        char[][] mapa = {{'#', '#', '#'}, {'#', 'P', '#'}, {'#', '#', '#' rock}};\n        System.out.println(localizarJugador(mapa));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string LocalizarJugador(char[,] mapa) {\n        for (int r = 0; r < mapa.GetLength(0); r++) {\n            for (int c = 0; c < mapa.GetLength(1); c++) {\n                if (mapa[r, c] == 'P') return $"📍 Héroe 'P' en Fila {r}, Columna {c}.";\n            }\n        }\n        return "❌ No encontrado.";\n    }\n    static void Main() {\n        char[,] mapa = { {'#', '#', '#'}, {'#', 'P', '#'}, {'#', '#', '#'} };\n        Console.WriteLine(LocalizarJugador(mapa));\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\n#include <string>\nstd::string localizarJugador(const std::vector<std::vector<char>>& mapa) {\n    for (size_t r = 0; r < mapa.size(); r++) {\n        for (size_t c = 0; c < mapa[r].size(); c++) {\n            if (mapa[r][c] == 'P') return "📍 Heroe en [" + std::to_string(r) + "," + std::to_string(c) + "]";\n        }\n    }\n    return "❌ No encontrado.";\n}\nint main() {\n    std::cout << localizarJugador({{'#','#','#'},{'#','P','#'},{'#','#','#'}}) << "\\n";\n    return 0;\n}`,
    javascript: `function localizarJugador(mapa) {\n    for (let r = 0; r < mapa.length; r++) {\n        for (let c = 0; c < mapa[r].length; c++) {\n            if (mapa[r][c] === "P") {\n                return \`📍 Héroe 'P' localizado en Fila \${r}, Columna \${c} ([\${r},\${c}]).\`;\n            }\n        }\n    }\n    return "❌ Jugador no encontrado.";\n}\nconst mapa = [["#", "#", "#"], ["#", "P", "#"], ["#", "#", "#"]];\nconsole.log(localizarJugador(mapa));`,
    php: `<?php\nfunction localizarJugador($mapa) {\n    foreach ($mapa as $r => $fila) {\n        foreach ($fila as $c => $celda) {\n            if ($celda === "P") return "📍 Héroe en Fila $r, Columna $c.";\n        }\n    }\n    return "❌ No encontrado.";\n}\n$mapa = [["#", "#", "#"], ["#", "P", "#"], ["#", "#", "#"]];\necho localizarJugador($mapa) . "\\n";\n?>`,
    pseint: `SubProceso res <- localizarJugador(f, c)\n    res <- "📍 Héroe 'P' localizado en Fila " + ConvertirATexto(f) + ", Columna " + ConvertirATexto(c)\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir localizarJugador(2, 2)\nFinAlgoritmo`
  },

  // 3. Contar Cofres del Tesoro ('C')
  3: {
    python: `def contarCofres(mapa):\n    cofres = 0\n    for fila in mapa:\n        for celda in fila:\n            if celda == "C":\n                cofres += 1\n    return f"💎 Total de cofres del tesoro en la mazmorra: {cofres} cofre(s)."\n\nmapa = [\n    ["#", "#", "#", "#", "#"],\n    ["#", "C", ".", "C", "#"],\n    ["#", ".", "C", ".", "#"],\n    ["#", "#", "#", "#", "#"]\n]\nprint(contarCofres(mapa))`,
    java: `public class Main {\n    public static String contarCofres(char[][] mapa) {\n        int count = 0;\n        for (int r = 0; r < mapa.length; r++) {\n            for (int c = 0; c < mapa[r].length; c++) {\n                if (mapa[r][c] == 'C') count++;\n            }\n        }\n        return "💎 Total de cofres: " + count + " cofre(s).";\n    }\n    public static void main(String[] args) {\n        char[][] mapa = {{'#','C','#'}, {'#','.','C'}, {'#','#','#'}};\n        System.out.println(contarCofres(mapa));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string ContarCofres(char[,] mapa) {\n        int count = 0;\n        for (int r = 0; r < mapa.GetLength(0); r++) {\n            for (int c = 0; c < mapa.GetLength(1); c++) {\n                if (mapa[r, c] == 'C') count++;\n            }\n        }\n        return $"💎 Total de cofres: {count} cofre(s).";\n    }\n    static void Main() {\n        char[,] mapa = { {'#','C','#'}, {'#','.','C'}, {'#','#','#'} };\n        Console.WriteLine(ContarCofres(mapa));\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\nint contarCofres(const std::vector<std::vector<char>>& mapa) {\n    int count = 0;\n    for (const auto& r : mapa) {\n        for (char c : r) if (c == 'C') count++;\n    }\n    return count;\n}\nint main() {\n    std::cout << "Cofres: " << contarCofres({{'#','C','#'},{'#','.','C'}}) << "\\n";\n    return 0;\n}`,
    javascript: `function contarCofres(mapa) {\n    let count = 0;\n    mapa.forEach(fila => {\n        fila.forEach(celda => { if (celda === "C") count++; });\n    });\n    return \`💎 Total de cofres del tesoro en la mazmorra: \${count} cofre(s).\`;\n}\nconst mapa = [["#", "C", "."], [".", "C", "C"], ["#", "#", "#"]];\nconsole.log(contarCofres(mapa));`,
    php: `<?php\nfunction contarCofres($mapa) {\n    $count = 0;\n    foreach ($mapa as $fila) {\n        foreach ($fila as $celda) if ($celda === "C") $count++;\n    }\n    return "💎 Total de cofres: $count cofre(s).";\n}\n$mapa = [["#", "C", "."], [".", "C", "C"], ["#", "#", "#"]];\necho contarCofres($mapa) . "\\n";\n?>`,
    pseint: `SubProceso res <- contarCofres(count)\n    res <- "💎 Total de cofres: " + ConvertirATexto(count) + " cofre(s)."\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir contarCofres(3)\nFinAlgoritmo`
  },

  // 4. Sumar Oro Recolectable en Mazmorra
  4: {
    python: `def sumarOroTotal(matrizOro):\n    totalOro = sum(sum(fila) for fila in matrizOro)\n    return f"🪙 Oro total en la sala: {totalOro} monedas de oro."\n\nsalaOro = [\n    [10, 50, 0],\n    [25, 100, 5],\n    [0,  20, 80]\n]\nprint(sumarOroTotal(salaOro))`,
    java: `public class Main {\n    public static String sumarOroTotal(int[][] matriz) {\n        int total = 0;\n        for (int[] fila : matriz) {\n            for (int val : fila) total += val;\n        }\n        return "🪙 Oro total en la sala: " + total + " monedas.";\n    }\n    public static void main(String[] args) {\n        int[][] sala = {{10, 50}, {25, 100}};\n        System.out.println(sumarOroTotal(sala));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string SumarOroTotal(int[,] matriz) {\n        int total = 0;\n        for (int r = 0; r < matriz.GetLength(0); r++) {\n            for (int c = 0; c < matriz.GetLength(1); c++) total += matriz[r, c];\n        }\n        return $"🪙 Oro total: {total} monedas.";\n    }\n    static void Main() {\n        int[,] sala = { {10, 50}, {25, 100} };\n        Console.WriteLine(SumarOroTotal(sala));\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\nint sumarOroTotal(const std::vector<std::vector<int>>& mat) {\n    int sum = 0;\n    for (const auto& r : mat) {\n        for (int v : r) sum += v;\n    }\n    return sum;\n}\nint main() {\n    std::cout << "Oro: " << sumarOroTotal({{10, 50}, {25, 100}}) << "\\n";\n    return 0;\n}`,
    javascript: `function sumarOroTotal(matrizOro) {\n    const total = matrizOro.flat().reduce((acc, v) => acc + v, 0);\n    return \`🪙 Oro total en la sala: \${total} monedas de oro.\`;\n}\nconst sala = [[10, 50, 0], [25, 100, 5], [0, 20, 80]];\nconsole.log(sumarOroTotal(sala));`,
    php: `<?php\nfunction sumarOroTotal($matrizOro) {\n    $total = 0;\n    foreach ($matrizOro as $fila) $total += array_sum($fila);\n    return "🪙 Oro total: $total monedas.";\n}\n$sala = [[10, 50, 0], [25, 100, 5], [0, 20, 80]];\necho sumarOroTotal($sala) . "\\n";\n?>`,
    pseint: `SubProceso res <- sumarOroTotal(total)\n    res <- "🪙 Oro total: " + ConvertirATexto(total) + " monedas."\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir sumarOroTotal(290)\nFinAlgoritmo`
  },

  // 5. Verificar Límites y Muro en Coordenada
  5: {
    python: `def esCeldaTransitable(mapa, f, c):\n    filas = len(mapa)\n    columnas = len(mapa[0])\n    if 0 <= f < filas and 0 <= c < columnas:\n        celda = mapa[f][c]\n        if celda != "#":\n            return f"✅ Celda [{f},{c}] transitable (Contiene '{celda}')."\n        else:\n            return f"🧱 Celda [{f},{c}] BLOQUEADA: Hay un muro indestructible."\n    else:\n        return f"🚫 Coordenada [{f},{c}] fuera de los límites de la mazmorra."\n\nmapa = [\n    ["#", "#", "#"],\n    ["#", ".", "#"],\n    ["#", "#", "#"]\n]\nprint(esCeldaTransitable(mapa, 1, 1))\nprint(esCeldaTransitable(mapa, 0, 1))\nprint(esCeldaTransitable(mapa, 5, 5))`,
    java: `public class Main {\n    public static String esCeldaTransitable(char[][] mapa, int f, int c) {\n        if (f >= 0 && f < mapa.length && c >= 0 && c < mapa[0].length) {\n            if (mapa[f][c] != '#') return "✅ Celda [" + f + "," + c + "] transitable.";\n            else return "🧱 Celda [" + f + "," + c + "] BLOQUEADA: Muro.";\n        }\n        return "🚫 Coordenada fuera de límites.";\n    }\n    public static void main(String[] args) {\n        char[][] mapa = {{'#','#','#'}, {'#','.','#'}, {'#','#','#'}};\n        System.out.println(esCeldaTransitable(mapa, 1, 1));\n        System.out.println(esCeldaTransitable(mapa, 0, 1));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string EsCeldaTransitable(char[,] mapa, int f, int c) {\n        if (f >= 0 && f < mapa.GetLength(0) && c >= 0 && c < mapa.GetLength(1)) {\n            if (mapa[f, c] != '#') return $"✅ Celda [{f},{c}] transitable.";\n            return $"🧱 Celda [{f},{c}] BLOQUEADA: Muro.";\n        }\n        return "🚫 Fuera de límites.";\n    }\n    static void Main() {\n        char[,] mapa = { {'#','#','#'}, {'#','.','#'}, {'#','#','#'} };\n        Console.WriteLine(EsCeldaTransitable(mapa, 1, 1));\n        Console.WriteLine(EsCeldaTransitable(mapa, 0, 1));\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\n#include <string>\nstd::string esCeldaTransitable(const std::vector<std::vector<char>>& m, int f, int c) {\n    if (f >= 0 && f < (int)m.size() && c >= 0 && c < (int)m[0].size()) {\n        return m[f][c] != '#' ? "✅ Transitable." : "🧱 Muro.";\n    }\n    return "🚫 Fuera de limites.";\n}\nint main() {\n    std::cout << esCeldaTransitable({{'#','#','#'},{'#','.','#'}}, 1, 1) << "\\n";\n    return 0;\n}`,
    javascript: `function esCeldaTransitable(mapa, f, c) {\n    if (f >= 0 && f < mapa.length && c >= 0 && c < mapa[0].length) {\n        return mapa[f][c] !== "#" ? \`✅ Celda [\${f},\${c}] transitable.\` : \`🧱 Celda [\${f},\${c}] BLOQUEADA: Muro.\`;\n    }\n    return \`🚫 Coordenada [\${f},\${c}] fuera de límites.\`;\n}\nconst mapa = [["#", "#", "#"], ["#", ".", "#"], ["#", "#", "#"]];\nconsole.log(esCeldaTransitable(mapa, 1, 1));\nconsole.log(esCeldaTransitable(mapa, 0, 1));`,
    php: `<?php\nfunction esCeldaTransitable($mapa, $f, $c) {\n    if (isset($mapa[$f][$c])) {\n        return $mapa[$f][$c] !== "#" ? "✅ Celda [$f,$c] transitable." : "🧱 Celda [$f,$c] Bloqueada.";\n    }\n    return "🚫 Fuera de límites.";\n}\n$mapa = [["#", "#", "#"], ["#", ".", "#"], ["#", "#", "#"]];\necho esCeldaTransitable($mapa, 1, 1) . "\\n";\n?>`,
    pseint: `SubProceso res <- esCeldaTransitable(muro, fuera)\n    Si fuera Entonces\n        res <- "🚫 Fuera de límites."\n    Sino\n        Si muro Entonces\n            res <- "🧱 Celda Bloqueada: Muro."\n        Sino\n            res <- "✅ Celda transitable."\n        FinSi\n    FinSi\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir esCeldaTransitable(Falso, Falso)\n    Escribir esCeldaTransitable(Verdadero, Falso)\nFinAlgoritmo`
  },

  // 6. Mover al Jugador a una Nueva Celda
  6: {
    python: `def moverJugador(mapa, fAct, cAct, fNueva, cNueva):\n    if mapa[fNueva][cNueva] != "#":\n        mapa[fAct][cAct] = "."\n        mapa[fNueva][cNueva] = "P"\n        return f"🚶 Héroe movido con éxito a [{fNueva},{cNueva}]."\n    else:\n        return "💥 ¡Colisión! No puedes caminar hacia un muro."\n\nmapa = [\n    ["#", "#", "#", "#"],\n    ["#", "P", ".", "#"],\n    ["#", "#", "#", "#"]\n]\nprint(moverJugador(mapa, 1, 1, 1, 2))\nfor fila in mapa:\n    print(" ".join(fila))`,
    java: `public class Main {\n    public static String moverJugador(char[][] mapa, int fAct, int cAct, int fN, int cN) {\n        if (mapa[fN][cN] != '#') {\n            mapa[fAct][cAct] = '.';\n            mapa[fN][cN] = 'P';\n            return "🚶 Héroe movido con éxito a [" + fN + "," + cN + "].";\n        }\n        return "💥 ¡Colisión con muro!";\n    }\n    public static void main(String[] args) {\n        char[][] mapa = {{'#','#','#'}, {'#','P','.'}, {'#','#','#'}};\n        System.out.println(moverJugador(mapa, 1, 1, 1, 2));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string MoverJugador(char[,] mapa, int fAct, int cAct, int fN, int cN) {\n        if (mapa[fN, cN] != '#') {\n            mapa[fAct, cAct] = '.';\n            mapa[fN, cN] = 'P';\n            return $"🚶 Héroe movido a [{fN},{cN}].";\n        }\n        return "💥 ¡Colisión con muro!";\n    }\n    static void Main() {\n        char[,] mapa = { {'#','#','#'}, {'#','P','.'}, {'#','#','#'} };\n        Console.WriteLine(MoverJugador(mapa, 1, 1, 1, 2));\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\n#include <string>\nstd::string moverJugador(std::vector<std::vector<char>>& m, int fa, int ca, int fn, int cn) {\n    if (m[fn][cn] != '#') {\n        m[fa][ca] = '.'; m[fn][cn] = 'P';\n        return "🚶 Movido a [" + std::to_string(fn) + "," + std::to_string(cn) + "]";\n    }\n    return "💥 Colision.";\n}\nint main() {\n    std::vector<std::vector<char>> m = {{'#','#','#'},{'#','P','.'},{'#','#','#'}};\n    std::cout << moverJugador(m, 1, 1, 1, 2) << "\\n";\n    return 0;\n}`,
    javascript: `function moverJugador(mapa, fAct, cAct, fN, cN) {\n    if (mapa[fN][cN] !== "#") {\n        mapa[fAct][cAct] = ".";\n        mapa[fN][cN] = "P";\n        return \`🚶 Héroe movido con éxito a [\${fN},\${cN}].\`;\n    }\n    return "💥 ¡Colisión! No puedes caminar hacia un muro.";\n}\nconst mapa = [["#", "#", "#", "#"], ["#", "P", ".", "#"], ["#", "#", "#", "#"]];\nconsole.log(moverJugador(mapa, 1, 1, 1, 2));`,
    php: `<?php\nfunction moverJugador(&$mapa, $fAct, $cAct, $fN, $cN) {\n    if ($mapa[$fN][$cN] !== "#") {\n        $mapa[$fAct][$cAct] = ".";\n        $mapa[$fN][$cN] = "P";\n        return "🚶 Héroe movido a [$fN,$cN].";\n    }\n    return "💥 ¡Colisión!";\n}\n$mapa = [["#", "#", "#"], ["#", "P", "."], ["#", "#", "#"]];\necho moverJugador($mapa, 1, 1, 1, 2) . "\\n";\n?>`,
    pseint: `SubProceso res <- moverJugador(fN, cN)\n    res <- "🚶 Héroe movido con éxito a [" + ConvertirATexto(fN) + "," + ConvertirATexto(cN) + "]."\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir moverJugador(1, 2)\nFinAlgoritmo`
  },

  // 7. Contar Enemigos en un Pasillo
  7: {
    python: `def contarEnemigosEnFila(mapa, numFila):\n    if 0 <= numFila < len(mapa):\n        enemigos = mapa[numFila].count("E")\n        return f"👾 Pasillo {numFila}: {enemigos} enemigo(s) al acecho."\n    return "🚫 Fila fuera de rango."\n\nmapa = [\n    ["#", "E", ".", "E", "#"],\n    ["#", ".", ".", ".", "#"],\n    ["#", "E", "E", "E", "#"]\n]\nprint(contarEnemigosEnFila(mapa, 0))\nprint(contarEnemigosEnFila(mapa, 2))`,
    java: `public class Main {\n    public static String contarEnemigosEnFila(char[][] mapa, int fila) {\n        if (fila >= 0 && fila < mapa.length) {\n            int count = 0;\n            for (char c : mapa[fila]) if (c == 'E') count++;\n            return "👾 Pasillo " + fila + ": " + count + " enemigo(s).";\n        }\n        return "🚫 Fila fuera de rango.";\n    }\n    public static void main(String[] args) {\n        char[][] mapa = {{'#','E','.','E','#'}, {'#','E','E','E','#'}};\n        System.out.println(contarEnemigosEnFila(mapa, 0));\n        System.out.println(contarEnemigosEnFila(mapa, 1));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string ContarEnemigosEnFila(char[,] mapa, int fila) {\n        int count = 0;\n        for (int c = 0; c < mapa.GetLength(1); c++) if (mapa[fila, c] == 'E') count++;\n        return $"👾 Pasillo {fila}: {count} enemigo(s).";\n    }\n    static void Main() {\n        char[,] mapa = { {'#','E','.','E','#'}, {'#','E','E','E','#'} };\n        Console.WriteLine(ContarEnemigosEnFila(mapa, 0));\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\nint contarEnemigosEnFila(const std::vector<std::vector<char>>& m, size_t fila) {\n    int count = 0;\n    for (char c : m[fila]) if (c == 'E') count++;\n    return count;\n}\nint main() {\n    std::cout << "Enemigos fila 0: " << contarEnemigosEnFila({{'#','E','.','E'}}, 0) << "\\n";\n    return 0;\n}`,
    javascript: `function contarEnemigosEnFila(mapa, numFila) {\n    if (numFila >= 0 && numFila < mapa.length) {\n        const count = mapa[numFila].filter(c => c === "E").length;\n        return \`👾 Pasillo \${numFila}: \${count} enemigo(s) al acecho.\`;\n    }\n    return "🚫 Fila fuera de rango.";\n}\nconst mapa = [["#", "E", ".", "E", "#"], ["#", ".", ".", ".", "#"], ["#", "E", "E", "E", "#"]];\nconsole.log(contarEnemigosEnFila(mapa, 0));\nconsole.log(contarEnemigosEnFila(mapa, 2));`,
    php: `<?php\nfunction contarEnemigosEnFila($mapa, $numFila) {\n    if (isset($mapa[$numFila])) {\n        $count = count(array_keys($mapa[$numFila], "E"));\n        return "👾 Pasillo $numFila: $count enemigo(s).";\n    }\n    return "🚫 Fila inválida.";\n}\n$mapa = [["#", "E", ".", "E", "#"], ["#", "E", "E", "E", "#"]];\necho contarEnemigosEnFila($mapa, 0) . "\\n";\n?>`,
    pseint: `SubProceso res <- contarEnemigos(count, fila)\n    res <- "👾 Pasillo " + ConvertirATexto(fila) + ": " + ConvertirATexto(count) + " enemigo(s)."\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir contarEnemigos(2, 0)\nFinAlgoritmo`
  },

  // 8. Detectar Peligro en Celdas Adyacentes
  8: {
    python: `def detectarEnemigoCercano(mapa, f, c):\n    direcciones = [(-1, 0), (1, 0), (0, -1), (0, 1)]  # Arriba, Abajo, Izquierda, Derecha\n    filas, cols = len(mapa), len(mapa[0])\n    for df, dc in direcciones:\n        nf, nc = f + df, c + dc\n        if 0 <= nf < filas and 0 <= nc < cols:\n            if mapa[nf][nc] == "E":\n                return f"⚠️ ¡PELIGRO INMINENTE! Enemigo detectado en adyacente [{nf},{nc}]."\n    return "🛡️ Celdas adyacentes despejadas. No hay enemigos inmediatos."\n\nmapa = [\n    [".", "E", "."],\n    [".", "P", "."],\n    [".", ".", "."]\n]\nprint(detectarEnemigoCercano(mapa, 1, 1))`,
    java: `public class Main {\n    public static String detectarEnemigoCercano(char[][] mapa, int f, int c) {\n        int[][] dirs = {{-1,0}, {1,0}, {0,-1}, {0,1}};\n        for (int[] d : dirs) {\n            int nf = f + d[0], nc = c + d[1];\n            if (nf >= 0 && nf < mapa.length && nc >= 0 && nc < mapa[0].length) {\n                if (mapa[nf][nc] == 'E') return "⚠️ ¡PELIGRO! Enemigo en [" + nf + "," + nc + "].";\n            }\n        }\n        return "🛡️ Seguro: No hay enemigos adyacentes.";\n    }\n    public static void main(String[] args) {\n        char[][] mapa = {{'.','E','.'}, {'.','P','.'}, {'.','.','.'}};\n        System.out.println(detectarEnemigoCercano(mapa, 1, 1));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string DetectarEnemigoCercano(char[,] mapa, int f, int c) {\n        int[] df = {-1, 1, 0, 0}; int[] dc = {0, 0, -1, 1};\n        for (int i = 0; i < 4; i++) {\n            int nf = f + df[i], nc = c + dc[i];\n            if (nf >= 0 && nf < mapa.GetLength(0) && nc >= 0 && nc < mapa.GetLength(1)) {\n                if (mapa[nf, nc] == 'E') return $"⚠️ ¡PELIGRO! Enemigo en [{nf},{nc}].";\n            }\n        }\n        return "🛡️ Despejado.";\n    }\n    static void Main() {\n        char[,] mapa = { {'.','E','.'}, {'.','P','.'}, {'.','.','.'} };\n        Console.WriteLine(DetectarEnemigoCercano(mapa, 1, 1));\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\n#include <string>\nstd::string detectarEnemigoCercano(const std::vector<std::vector<char>>& m, int f, int c) {\n    int df[] = {-1, 1, 0, 0}; int dc[] = {0, 0, -1, 1};\n    for (int i = 0; i < 4; i++) {\n        int nf = f + df[i], nc = c + dc[i];\n        if (nf >= 0 && nf < (int)m.size() && nc >= 0 && nc < (int)m[0].size()) {\n            if (m[nf][nc] == 'E') return "⚠️ Enemigo adyacente.";\n        }\n    }\n    return "🛡️ Seguro.";\n}\nint main() {\n    std::cout << detectarEnemigoCercano({{'.','E','.'},{'.','P','.'}}, 1, 1) << "\\n";\n    return 0;\n}`,
    javascript: `function detectarEnemigoCercano(mapa, f, c) {\n    const dirs = [[-1, 0], [1, 0], [0, -1], [0, 1]];\n    for (const [df, dc] of dirs) {\n        const nf = f + df, nc = c + dc;\n        if (nf >= 0 && nf < mapa.length && nc >= 0 && nc < mapa[0].length) {\n            if (mapa[nf][nc] === "E") return \`⚠️ ¡PELIGRO INMINENTE! Enemigo en [\${nf},\${nc}].\`;\n        }\n    }\n    return "🛡️ Celdas adyacentes despejadas.";\n}\nconst mapa = [[".", "E", "."], [".", "P", "."], [".", ".", "."]];\nconsole.log(detectarEnemigoCercano(mapa, 1, 1));`,
    php: `<?php\nfunction detectarEnemigoCercano($mapa, $f, $c) {\n    $dirs = [[-1,0], [1,0], [0,-1], [0,1]];\n    foreach ($dirs as [$df, $dc]) {\n        $nf = $f + $df; $nc = $c + $dc;\n        if (isset($mapa[$nf][$nc]) && $mapa[$nf][$nc] === "E") {\n            return "⚠️ ¡PELIGRO! Enemigo en [$nf,$nc].";\n        }\n    }\n    return "🛡️ Seguro.";\n}\n$mapa = [[".", "E", "."], [".", "P", "."], [".", ".", "."]];\necho detectarEnemigoCercano($mapa, 1, 1) . "\\n";\n?>`,
    pseint: `SubProceso res <- detectarPeligro(hayPeligro)\n    Si hayPeligro Entonces\n        res <- "⚠️ ¡PELIGRO INMINENTE! Enemigo adyacente."\n    Sino\n        res <- "🛡️ Celdas adyacentes despejadas."\n    FinSi\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir detectarPeligro(Verdadero)\nFinAlgoritmo`
  },

  // 9. Desactivar Todas las Trampas ('T')
  9: {
    python: `def desactivarTrampas(mapa):\n    desactivadas = 0\n    for r in range(len(mapa)):\n        for c in range(len(mapa[0])):\n            if mapa[r][c] == "T":\n                mapa[r][c] = "."\n                desactivadas += 1\n    return f"🔧 {desactivadas} trampa(s) neutralizadas con el kit de pícaro."\n\nmapa = [\n    ["#", "T", "#"],\n    [".", "T", "."],\n    ["#", "#", "T"]\n]\nprint(desactivarTrampas(mapa))\nfor f in mapa: print(" ".join(f))`,
    java: `public class Main {\n    public static String desactivarTrampas(char[][] mapa) {\n        int count = 0;\n        for (int r = 0; r < mapa.length; r++) {\n            for (int c = 0; c < mapa[r].length; c++) {\n                if (mapa[r][c] == 'T') {\n                    mapa[r][c] = '.';\n                    count++;\n                }\n            }\n        }\n        return "🔧 " + count + " trampa(s) neutralizadas.";\n    }\n    public static void main(String[] args) {\n        char[][] mapa = {{'#','T','#'}, {'.','T','.'}};\n        System.out.println(desactivarTrampas(mapa));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string DesactivarTrampas(char[,] mapa) {\n        int count = 0;\n        for (int r = 0; r < mapa.GetLength(0); r++) {\n            for (int c = 0; c < mapa.GetLength(1); c++) {\n                if (mapa[r, c] == 'T') { mapa[r, c] = '.'; count++; }\n            }\n        }\n        return $"🔧 {count} trampa(s) neutralizadas.";\n    }\n    static void Main() {\n        char[,] mapa = { {'#','T','#'}, {'.','T','.'} };\n        Console.WriteLine(DesactivarTrampas(mapa));\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\nint desactivarTrampas(std::vector<std::vector<char>>& m) {\n    int count = 0;\n    for (auto& r : m) {\n        for (char& c : r) if (c == 'T') { c = '.'; count++; }\n    }\n    return count;\n}\nint main() {\n    std::vector<std::vector<char>> m = {{'#','T','#'},{'.','T','.'}};\n    std::cout << "Trampas quitadas: " << desactivarTrampas(m) << "\\n";\n    return 0;\n}`,
    javascript: `function desactivarTrampas(mapa) {\n    let count = 0;\n    for (let r = 0; r < mapa.length; r++) {\n        for (let c = 0; c < mapa[r].length; c++) {\n            if (mapa[r][c] === "T") { mapa[r][c] = "."; count++; }\n        }\n    }\n    return \`🔧 \${count} trampa(s) neutralizadas con el kit de pícaro.\`;\n}\nconst mapa = [["#", "T", "#"], [".", "T", "."]];\nconsole.log(desactivarTrampas(mapa));`,
    php: `<?php\nfunction desactivarTrampas(&$mapa) {\n    $count = 0;\n    foreach ($mapa as &$fila) {\n        foreach ($fila as &$c) if ($c === "T") { $c = "."; $count++; }\n    }\n    return "🔧 $count trampa(s) neutralizadas.";\n}\n$mapa = [["#", "T", "#"], [".", "T", "."]];\necho desactivarTrampas($mapa) . "\\n";\n?>`,
    pseint: `SubProceso res <- desactivarTrampas(count)\n    res <- "🔧 " + ConvertirATexto(count) + " trampa(s) neutralizadas."\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir desactivarTrampas(3)\nFinAlgoritmo`
  },

  // 10. Contar Muros Alrededor de una Celda
  10: {
    python: `def contarMurosAlrededor(mapa, f, c):\n    direcciones = [(-1, 0), (1, 0), (0, -1), (0, 1)]\n    muros = 0\n    for df, dc in direcciones:\n        nf, nc = f + df, c + dc\n        if 0 <= nf < len(mapa) and 0 <= nc < len(mapa[0]):\n            if mapa[nf][nc] == "#":\n                muros += 1\n    if muros == 3:\n        return f"🧱 [{muros} muros]: ¡CALLEJÓN SIN SALIDA! Solo tienes 1 salida."\n    return f"🧱 [{muros} muros]: La celda tiene {4 - muros} accesos libres."\n\nmapa = [\n    ["#", "#", "#"],\n    ["#", ".", "."],\n    ["#", "#", "#"]\n]\nprint(contarMurosAlrededor(mapa, 1, 1))`,
    java: `public class Main {\n    public static String contarMurosAlrededor(char[][] mapa, int f, int c) {\n        int[][] dirs = {{-1,0}, {1,0}, {0,-1}, {0,1}};\n        int muros = 0;\n        for (int[] d : dirs) {\n            int nf = f + d[0], nc = c + d[1];\n            if (nf >= 0 && nf < mapa.length && nc >= 0 && nc < mapa[0].length) {\n                if (mapa[nf][nc] == '#') muros++;\n            }\n        }\n        return "🧱 [" + muros + " muros adyacentes].";\n    }\n    public static void main(String[] args) {\n        char[][] mapa = {{'#','#','#'}, {'#','.','.'}, {'#','#','#'}};\n        System.out.println(contarMurosAlrededor(mapa, 1, 1));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string ContarMurosAlrededor(char[,] mapa, int f, int c) {\n        int[] df = {-1, 1, 0, 0}; int[] dc = {0, 0, -1, 1};\n        int muros = 0;\n        for (int i = 0; i < 4; i++) {\n            int nf = f + df[i], nc = c + dc[i];\n            if (nf >= 0 && nf < mapa.GetLength(0) && nc >= 0 && nc < mapa.GetLength(1)) {\n                if (mapa[nf, nc] == '#') muros++;\n            }\n        }\n        return $"🧱 [{muros} muros]: {4 - muros} accesos libres.";\n    }\n    static void Main() {\n        char[,] mapa = { {'#','#','#'}, {'#','.','.'}, {'#','#','#'} };\n        Console.WriteLine(ContarMurosAlrededor(mapa, 1, 1));\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\nint contarMurosAlrededor(const std::vector<std::vector<char>>& m, int f, int c) {\n    int df[] = {-1, 1, 0, 0}; int dc[] = {0, 0, -1, 1}; int muros = 0;\n    for (int i = 0; i < 4; i++) {\n        int nf = f + df[i], nc = c + dc[i];\n        if (nf >= 0 && nf < (int)m.size() && nc >= 0 && nc < (int)m[0].size()) {\n            if (m[nf][nc] == '#') muros++;\n        }\n    }\n    return muros;\n}\nint main() {\n    std::cout << "Muros: " << contarMurosAlrededor({{'#','#','#'},{'#','.','.'}}, 1, 1) << "\\n";\n    return 0;\n}`,
    javascript: `function contarMurosAlrededor(mapa, f, c) {\n    const dirs = [[-1, 0], [1, 0], [0, -1], [0, 1]];\n    let muros = 0;\n    dirs.forEach(([df, dc]) => {\n        const nf = f + df, nc = c + dc;\n        if (nf >= 0 && nf < mapa.length && nc >= 0 && nc < mapa[0].length) {\n            if (mapa[nf][nc] === "#") muros++;\n        }\n    });\n    return \`🧱 [\${muros} muros]: La celda tiene \${4 - muros} accesos libres.\`;\n}\nconst mapa = [["#", "#", "#"], ["#", ".", "."], ["#", "#", "#"]];\nconsole.log(contarMurosAlrededor(mapa, 1, 1));`,
    php: `<?php\nfunction contarMurosAlrededor($mapa, $f, $c) {\n    $dirs = [[-1,0], [1,0], [0,-1], [0,1]];\n    $muros = 0;\n    foreach ($dirs as [$df, $dc]) {\n        $nf = $f + $df; $nc = $c + $dc;\n        if (isset($mapa[$nf][$nc]) && $mapa[$nf][$nc] === "#") $muros++;\n    }\n    return "🧱 [$muros muros]: " . (4 - $muros) . " accesos libres.";\n}\n$mapa = [["#", "#", "#"], ["#", ".", "."], ["#", "#", "#"]];\necho contarMurosAlrededor($mapa, 1, 1) . "\\n";\n?>`,
    pseint: `SubProceso res <- contarMuros(muros)\n    res <- "🧱 [" + ConvertirATexto(muros) + " muros]: " + ConvertirATexto(4 - muros) + " accesos libres."\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir contarMuros(3)\nFinAlgoritmo`
  },

  // 11. Generar Sala Perimetral con Muros
  11: {
    python: `def crearSalaVacia(filas, columnas):\n    matriz = []\n    for r in range(filas):\n        fila = []\n        for c in range(columnas):\n            if r == 0 or r == filas - 1 or c == 0 or c == columnas - 1:\n                fila.append("#")\n            else:\n                fila.append(".")\n        matriz.append(fila)\n    return matriz\n\nsala = crearSalaVacia(4, 5)\nfor f in sala: print(" ".join(f))`,
    java: `public class Main {\n    public static char[][] crearSalaVacia(int filas, int cols) {\n        char[][] m = new char[filas][cols];\n        for (int r = 0; r < filas; r++) {\n            for (int c = 0; c < cols; c++) {\n                if (r == 0 || r == filas - 1 || c == 0 || c == cols - 1) m[r][c] = '#';\n                else m[r][c] = '.';\n            }\n        }\n        return m;\n    }\n    public static void main(String[] args) {\n        char[][] s = crearSalaVacia(4, 5);\n        for (char[] r : s) System.out.println(new String(r));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static char[,] CrearSalaVacia(int filas, int cols) {\n        char[,] m = new char[filas, cols];\n        for (int r = 0; r < filas; r++) {\n            for (int c = 0; c < cols; c++) {\n                m[r, c] = (r == 0 || r == filas - 1 || c == 0 || c == cols - 1) ? '#' : '.';\n            }\n        }\n        return m;\n    }\n    static void Main() {\n        var s = CrearSalaVacia(4, 5);\n        Console.WriteLine($"Sala {s.GetLength(0)}x{s.GetLength(1)} generada.");\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\nstd::vector<std::vector<char>> crearSalaVacia(int f, int c) {\n    std::vector<std::vector<char>> m(f, std::vector<char>(c, '.'));\n    for (int r = 0; r < f; r++) {\n        for (int col = 0; col < c; col++) {\n            if (r == 0 || r == f - 1 || col == 0 || col == c - 1) m[r][col] = '#';\n        }\n    }\n    return m;\n}\nint main() {\n    auto s = crearSalaVacia(3, 4);\n    for (const auto& r : s) { for (char ch : r) std::cout << ch; std::cout << "\\n"; }\n    return 0;\n}`,
    javascript: `function crearSalaVacia(filas, columnas) {\n    return Array.from({ length: filas }, (_, r) =>\n        Array.from({ length: columnas }, (_, c) =>\n            (r === 0 || r === filas - 1 || c === 0 || c === columnas - 1) ? "#" : "."\n        )\n    );\n}\nconst sala = crearSalaVacia(4, 5);\nsala.forEach(f => console.log(f.join(" ")));`,
    php: `<?php\nfunction crearSalaVacia($filas, $columnas) {\n    $m = [];\n    for ($r = 0; $r < $filas; $r++) {\n        for ($c = 0; $c < $columnas; $c++) {\n            $m[$r][$c] = ($r === 0 || $r === $filas - 1 || $c === 0 || $c === $columnas - 1) ? "#" : ".";\n        }\n    }\n    return $m;\n}\n$s = crearSalaVacia(4, 5);\nforeach ($s as $f) echo implode(" ", $f) . "\\n";\n?>`,
    pseint: `SubProceso crearSalaVacia(filas, columnas)\n    Escribir "🏰 Sala de ", filas, "x", columnas, " generada con éxito."\nFinSubProceso\n\nAlgoritmo Principal\n    crearSalaVacia(4, 5)\nFinAlgoritmo`
  },

  // 12. Lanzar Ataque en Cruz (Área de Efecto)
  12: {
    python: `def lanzarAtaqueEnCruz(mapa, f, c):\n    direcciones = [(0, 0), (-1, 0), (1, 0), (0, -1), (0, 1)]\n    for df, dc in direcciones:\n        nf, nc = f + df, c + dc\n        if 0 <= nf < len(mapa) and 0 <= nc < len(mapa[0]):\n            if mapa[nf][nc] != "#":\n                mapa[nf][nc] = "*"\n    return f"💥 ¡Hechizo en Cruz desatado en [{f},{c}]!"\n\nmapa = [\n    [".", ".", "."],\n    [".", ".", "."],\n    [".", ".", "."]\n]\nprint(lanzarAtaqueEnCruz(mapa, 1, 1))\nfor fila in mapa: print(" ".join(fila))`,
    java: `public class Main {\n    public static void lanzarAtaqueEnCruz(char[][] mapa, int f, int c) {\n        int[][] dirs = {{0,0}, {-1,0}, {1,0}, {0,-1}, {0,1}};\n        for (int[] d : dirs) {\n            int nf = f + d[0], nc = c + d[1];\n            if (nf >= 0 && nf < mapa.length && nc >= 0 && nc < mapa[0].length) {\n                if (mapa[nf][nc] != '#') mapa[nf][nc] = '*';\n            }\n        }\n    }\n    public static void main(String[] args) {\n        char[][] m = {{'.','.','.'}, {'.','.','.'}, {'.','.','.'}};\n        lanzarAtaqueEnCruz(m, 1, 1);\n        for (char[] r : m) System.out.println(new String(r));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static void LanzarAtaqueEnCruz(char[,] mapa, int f, int c) {\n        int[] df = {0, -1, 1, 0, 0}; int[] dc = {0, 0, 0, -1, 1};\n        for (int i = 0; i < 5; i++) {\n            int nf = f + df[i], nc = c + dc[i];\n            if (nf >= 0 && nf < mapa.GetLength(0) && nc >= 0 && nc < mapa.GetLength(1)) {\n                if (mapa[nf, nc] != '#') mapa[nf, nc] = '*';\n            }\n        }\n    }\n    static void Main() {\n        char[,] m = { {'.','.','.'}, {'.','.','.'}, {'.','.','.'} };\n        LanzarAtaqueEnCruz(m, 1, 1);\n        Console.WriteLine("💥 Ataque ejecutado.");\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\nvoid lanzarAtaqueEnCruz(std::vector<std::vector<char>>& m, int f, int c) {\n    int df[] = {0, -1, 1, 0, 0}; int dc[] = {0, 0, 0, -1, 1};\n    for (int i = 0; i < 5; i++) {\n        int nf = f + df[i], nc = c + dc[i];\n        if (nf >= 0 && nf < (int)m.size() && nc >= 0 && nc < (int)m[0].size()) {\n            if (m[nf][nc] != '#') m[nf][nc] = '*';\n        }\n    }\n}\nint main() {\n    std::vector<std::vector<char>> m = {{'.','.','.'},{'.','.','.'},{'.','.','.'}};\n    lanzarAtaqueEnCruz(m, 1, 1);\n    std::cout << "💥 Ataque en cruz marcado.\\n";\n    return 0;\n}`,
    javascript: `function lanzarAtaqueEnCruz(mapa, f, c) {\n    const dirs = [[0, 0], [-1, 0], [1, 0], [0, -1], [0, 1]];\n    dirs.forEach(([df, dc]) => {\n        const nf = f + df, nc = c + dc;\n        if (nf >= 0 && nf < mapa.length && nc >= 0 && nc < mapa[0].length) {\n            if (mapa[nf][nc] !== "#") mapa[nf][nc] = "*";\n        }\n    });\n    return \`💥 ¡Hechizo en Cruz desatado en [\${f},\${c}]!\`;\n}\nconst mapa = [[".", ".", "."], [".", ".", "."], [".", ".", "."]];\nconsole.log(lanzarAtaqueEnCruz(mapa, 1, 1));\nmapa.forEach(r => console.log(r.join(" ")));`,
    php: `<?php\nfunction lanzarAtaqueEnCruz(&$mapa, $f, $c) {\n    $dirs = [[0,0], [-1,0], [1,0], [0,-1], [0,1]];\n    foreach ($dirs as [$df, $dc]) {\n        $nf = $f + $df; $nc = $c + $dc;\n        if (isset($mapa[$nf][$nc]) && $mapa[$nf][$nc] !== "#") $mapa[$nf][$nc] = "*";\n    }\n}\n$mapa = [[".", ".", "."], [".", ".", "."], [".", ".", "."]];\nlanzarAtaqueEnCruz($mapa, 1, 1);\nforeach ($mapa as $r) echo implode(" ", $r) . "\\n";\n?>`,
    pseint: `SubProceso lanzarAtaqueEnCruz(f, c)\n    Escribir "💥 ¡Hechizo en Cruz desatado en [", f, ",", c, "]!"\nFinSubProceso\n\nAlgoritmo Principal\n    lanzarAtaqueEnCruz(1, 1)\nFinAlgoritmo`
  },

  // 13. Comprobar si una Sala es Simétrica
  13: {
    python: `def esSalaSimetrica(mapa):\n    for fila in mapa:\n        if fila != fila[::-1]:\n            return "❌ La sala no es simétrica horizontalmente."\n    return "🪞 ¡Sala Espejo detectada! Simetría bilateral perfecta."\n\nsala1 = [\n    ["#", ".", "P", ".", "#"],\n    ["#", "C", ".", "C", "#"],\n    ["#", "#", "#", "#", "#"]\n]\nprint(esSalaSimetrica(sala1))`,
    java: `public class Main {\n    public static boolean esSalaSimetrica(char[][] mapa) {\n        for (char[] fila : mapa) {\n            int i = 0, j = fila.length - 1;\n            while (i < j) {\n                if (fila[i] != fila[j]) return false;\n                i++; j--;\n            }\n        }\n        return true;\n    }\n    public static void main(String[] args) {\n        char[][] m = {{'#','.','P','.','#'}, {'#','C','.','C','#'}};\n        System.out.println("Simétrica: " + esSalaSimetrica(m));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static bool EsSalaSimetrica(char[,] mapa) {\n        int filas = mapa.GetLength(0), cols = mapa.GetLength(1);\n        for (int r = 0; r < filas; r++) {\n            for (int c = 0; c < cols / 2; c++) {\n                if (mapa[r, c] != mapa[r, cols - 1 - c]) return false;\n            }\n        }\n        return true;\n    }\n    static void Main() {\n        char[,] m = { {'#','.','P','.','#'}, {'#','C','.','C','#'} };\n        Console.WriteLine($"Simétrica: {EsSalaSimetrica(m)}");\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\nbool esSalaSimetrica(const std::vector<std::vector<char>>& m) {\n    for (const auto& r : m) {\n        size_t i = 0, j = r.size() - 1;\n        while (i < j) { if (r[i] != r[j]) return false; i++; j--; }\n    }\n    return true;\n}\nint main() {\n    std::cout << "Espejo: " << esSalaSimetrica({{'#','.','#'},{'C','.','C'}}) << "\\n";\n    return 0;\n}`,
    javascript: `function esSalaSimetrica(mapa) {\n    const simetrica = mapa.every(fila => fila.join("") === [...fila].reverse().join(""));\n    return simetrica ? "🪞 ¡Sala Espejo! Simetría bilateral perfecta." : "❌ La sala no es simétrica.";\n}\nconst sala = [["#", ".", "P", ".", "#"], ["#", "C", ".", "C", "#"]];\nconsole.log(esSalaSimetrica(sala));`,
    php: `<?php\nfunction esSalaSimetrica($mapa) {\n    foreach ($mapa as $fila) {\n        if ($fila !== array_reverse($fila)) return false;\n    }\n    return true;\n}\n$sala = [["#", ".", "P", ".", "#"], ["#", "C", ".", "C", "#"]];\necho (esSalaSimetrica($sala) ? "🪞 Simétrica" : "❌ No simétrica") . "\\n";\n?>`,
    pseint: `SubProceso res <- esSalaSimetrica(espejo)\n    Si espejo Entonces\n        res <- "🪞 ¡Sala Espejo! Simetría perfecta."\n    Sino\n        res <- "❌ La sala no es simétrica."\n    FinSi\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir esSalaSimetrica(Verdadero)\nFinAlgoritmo`
  },

  // 14. Rotar Mapa de la Mazmorra 90 Grados
  14: {
    python: `def rotarMapa90Grados(mapa):\n    # Transponer e invertir filas (Rotación 90° horario)\n    rotado = [list(fila) for fila in zip(*mapa[::-1])]\n    return rotado\n\nmapa = [\n    ["1", "2"],\n    ["3", "4"]\n]\nrotado = rotarMapa90Grados(mapa)\nprint("Original:", mapa)\nprint("Rotado 90°:", rotado)`,
    java: `import java.util.*;\npublic class Main {\n    public static char[][] rotar90(char[][] m) {\n        int n = m.length;\n        char[][] r = new char[n][n];\n        for (int i = 0; i < n; i++) {\n            for (int j = 0; j < n; j++) r[j][n - 1 - i] = m[i][j];\n        }\n        return r;\n    }\n    public static void main(String[] args) {\n        char[][] m = {{'1','2'}, {'3','4'}};\n        char[][] rot = rotar90(m);\n        for (char[] row : rot) System.out.println(Arrays.toString(row));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static char[,] Rotar90(char[,] m) {\n        int n = m.GetLength(0);\n        char[,] r = new char[n, n];\n        for (int i = 0; i < n; i++) {\n            for (int j = 0; j < n; j++) r[j, n - 1 - i] = m[i, j];\n        }\n        return r;\n    }\n    static void Main() {\n        char[,] m = { {'1','2'}, {'3','4'} };\n        var r = Rotar90(m);\n        Console.WriteLine($"Rotado: [{r[0,0]},{r[0,1]}],[{r[1,0]},{r[1,1]}]");\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\nstd::vector<std::vector<char>> rotar90(const std::vector<std::vector<char>>& m) {\n    int n = m.size();\n    std::vector<std::vector<char>> r(n, std::vector<char>(n));\n    for (int i = 0; i < n; i++)\n        for (int j = 0; j < n; j++) r[j][n - 1 - i] = m[i][j];\n    return r;\n}\nint main() {\n    auto r = rotar90({{'1','2'},{'3','4'}});\n    std::cout << r[0][0] << " " << r[0][1] << "\\n";\n    return 0;\n}`,
    javascript: `function rotarMapa90Grados(mapa) {\n    const N = mapa.length;\n    return mapa[0].map((_, colIndex) => mapa.map(row => row[colIndex]).reverse());\n}\nconst mapa = [["1", "2"], ["3", "4"]];\nconsole.log("Rotado 90°:", rotarMapa90Grados(mapa));`,
    php: `<?php\nfunction rotarMapa90Grados($mapa) {\n    $n = count($mapa);\n    $res = [];\n    for ($i = 0; $i < $n; $i++) {\n        for ($j = 0; $j < $n; $j++) $res[$j][$n - 1 - $i] = $mapa[$i][$j];\n    }\n    return $res;\n}\n$m = [["1", "2"], ["3", "4"]];\nprint_r(rotarMapa90Grados($m));\n?>`,
    pseint: `SubProceso rotarMapa90()\n    Escribir "🔄 Mapa rotado 90 grados en sentido horario."\nFinSubProceso\n\nAlgoritmo Principal\n    rotarMapa90()\nFinAlgoritmo`
  },

  // 15. Intercambiar Dos Pasillos/Filas del Mapa
  15: {
    python: `def intercambiarFilas(mapa, f1, f2):\n    if 0 <= f1 < len(mapa) and 0 <= f2 < len(mapa):\n        mapa[f1], mapa[f2] = mapa[f2], mapa[f1]\n        return f"🔄 Mecanismo activado: Pasillos {f1} y {f2} intercambiados."\n    return "⚠️ Filas fuera de límites."\n\nmapa = [\n    ["#", "A", "#"],\n    ["#", "B", "#"],\n    ["#", "C", "#"]\n]\nprint(intercambiarFilas(mapa, 0, 2))\nfor f in mapa: print(" ".join(f))`,
    java: `public class Main {\n    public static void intercambiarFilas(char[][] mapa, int f1, int f2) {\n        char[] temp = mapa[f1];\n        mapa[f1] = mapa[f2];\n        mapa[f2] = temp;\n        System.out.println("🔄 Filas " + f1 + " y " + f2 + " intercambiadas.");\n    }\n    public static void main(String[] args) {\n        char[][] m = {{'#','A','#'}, {'#','B','#'}, {'#','C','#'}};\n        intercambiarFilas(m, 0, 2);\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static void Main() {\n        string[] mapa = { "# A #", "# B #", "# C #" };\n        (mapa[0], mapa[2]) = (mapa[2], mapa[0]);\n        Console.WriteLine($"🔄 Intercambiado: {string.Join(" | ", mapa)}");\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\n#include <utility>\nvoid intercambiarFilas(std::vector<std::vector<char>>& m, int f1, int f2) {\n    std::swap(m[f1], m[f2]);\n}\nint main() {\n    std::vector<std::vector<char>> m = {{'A'},{'B'},{'C'}};\n    intercambiarFilas(m, 0, 2);\n    std::cout << m[0][0] << " " << m[2][0] << "\\n";\n    return 0;\n}`,
    javascript: `function intercambiarFilas(mapa, f1, f2) {\n    if (f1 >= 0 && f1 < mapa.length && f2 >= 0 && f2 < mapa.length) {\n        [mapa[f1], mapa[f2]] = [mapa[f2], mapa[f1]];\n        return \`🔄 Pasillos \${f1} y \${f2} intercambiados.\`;\n    }\n    return "⚠️ Filas fuera de límites.";\n}\nconst mapa = [["#", "A", "#"], ["#", "B", "#"], ["#", "C", "#"]];\nconsole.log(intercambiarFilas(mapa, 0, 2));`,
    php: `<?php\nfunction intercambiarFilas(&$mapa, $f1, $f2) {\n    $temp = $mapa[$f1];\n    $mapa[$f1] = $mapa[$f2];\n    $mapa[$f2] = $temp;\n}\n$mapa = [["#", "A", "#"], ["#", "B", "#"], ["#", "C", "#"]];\nintercambiarFilas($mapa, 0, 2);\nprint_r($mapa);\n?>`,
    pseint: `SubProceso intercambiarPasillos(f1, f2)\n    Escribir "🔄 Pasillo ", f1, " y Pasillo ", f2, " intercambiados."\nFinSubProceso\n\nAlgoritmo Principal\n    intercambiarPasillos(0, 2)\nFinAlgoritmo`
  },

  // 16. Revelar Niebla de Guerra en Radio 3x3
  16: {
    python: `def revelarNiebla(mapaVisible, mapaReal, f, c):\n    filas, cols = len(mapaReal), len(mapaReal[0])\n    for df in range(-1, 2):\n        for dc in range(-1, 2):\n            nf, nc = f + df, c + dc\n            if 0 <= nf < filas and 0 <= nc < cols:\n                mapaVisible[nf][nc] = mapaReal[nf][nc]\n    return f"🕯️ Niebla de guerra despejada alrededor de [{f},{c}]."\n\nmapaReal = [["#", "#", "#"], ["#", "C", "#"], ["#", ".", "#"]]\nmapaVis = [["?", "?", "?"], ["?", "?", "?"], ["?", "?", "?"]]\nprint(revelarNiebla(mapaVis, mapaReal, 1, 1))\nfor fila in mapaVis: print(" ".join(fila))`,
    java: `public class Main {\n    public static void revelarNiebla(char[][] vis, char[][] real, int f, int c) {\n        for (int df = -1; df <= 1; df++) {\n            for (int dc = -1; dc <= 1; dc++) {\n                int nf = f + df, nc = c + dc;\n                if (nf >= 0 && nf < real.length && nc >= 0 && nc < real[0].length) {\n                    vis[nf][nc] = real[nf][nc];\n                }\n            }\n        }\n    }\n    public static void main(String[] args) {\n        char[][] real = {{'#','#','#'}, {'#','C','#'}, {'#','.','#'}};\n        char[][] vis = {{'?','?','?'}, {'?','?','?'}, {'?','?','?'}};\n        revelarNiebla(vis, real, 1, 1);\n        for (char[] r : vis) System.out.println(new String(r));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static void RevelarNiebla(char[,] vis, char[,] realM, int f, int c) {\n        for (int df = -1; df <= 1; df++) {\n            for (int dc = -1; dc <= 1; dc++) {\n                int nf = f + df, nc = c + dc;\n                if (nf >= 0 && nf < realM.GetLength(0) && nc >= 0 && nc < realM.GetLength(1)) {\n                    vis[nf, nc] = realM[nf, nc];\n                }\n            }\n        }\n    }\n    static void Main() {\n        Console.WriteLine("🕯️ Niebla despejada.");\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\nvoid revelarNiebla(std::vector<std::vector<char>>& vis, const std::vector<std::vector<char>>& realM, int f, int c) {\n    for (int df = -1; df <= 1; df++) {\n        for (int dc = -1; dc <= 1; dc++) {\n            int nf = f + df, nc = c + dc;\n            if (nf >= 0 && nf < (int)realM.size() && nc >= 0 && nc < (int)realM[0].size()) vis[nf][nc] = realM[nf][nc];\n        }\n    }\n}\nint main() {\n    std::cout << "🕯️ Niebla despejada.\\n";\n    return 0;\n}`,
    javascript: `function revelarNiebla(mapaVisible, mapaReal, f, c) {\n    for (let df = -1; df <= 1; df++) {\n        for (let dc = -1; dc <= 1; dc++) {\n            const nf = f + df, nc = c + dc;\n            if (nf >= 0 && nf < mapaReal.length && nc >= 0 && nc < mapaReal[0].length) {\n                mapaVisible[nf][nc] = mapaReal[nf][nc];\n            }\n        }\n    }\n    return \`🕯️ Niebla despejada en radio 3x3 alrededor de [\${f},\${c}].\`;\n}\nconst realM = [["#", "#", "#"], ["#", "C", "#"], ["#", ".", "#"]];\nconst vis = [["?", "?", "?"], ["?", "?", "?"], ["?", "?", "?"]];\nconsole.log(revelarNiebla(vis, realM, 1, 1));\nvis.forEach(r => console.log(r.join(" ")));`,
    php: `<?php\nfunction revelarNiebla(&$vis, $realM, $f, $c) {\n    for ($df = -1; $df <= 1; $df++) {\n        for ($dc = -1; $dc <= 1; $dc++) {\n            $nf = $f + $df; $nc = $c + $dc;\n            if (isset($realM[$nf][$nc])) $vis[$nf][$nc] = $realM[$nf][$nc];\n        }\n    }\n}\n?>`,
    pseint: `SubProceso revelarNiebla(f, c)\n    Escribir "🕯️ Niebla despejada alrededor de [", f, ",", c, "]."\nFinSubProceso\n\nAlgoritmo Principal\n    revelarNiebla(1, 1)\nFinAlgoritmo`
  },

  // 17. Calcular Distancia Manhattan hasta la Salida
  17: {
    python: `def calcularDistanciaSalida(fJugador, cJugador, fSalida, cSalida):\n    distancia = abs(fJugador - fSalida) + abs(cJugador - cSalida)\n    return f"🧭 Distancia hasta la salida 'X': {distancia} pasos de movimiento."\n\nprint(calcularDistanciaSalida(1, 1, 4, 4))\nprint(calcularDistanciaSalida(0, 0, 3, 2))`,
    java: `public class Main {\n    public static String calcularDistanciaSalida(int fj, int cj, int fs, int cs) {\n        int d = Math.abs(fj - fs) + Math.abs(cj - cs);\n        return "🧭 Distancia hasta la salida: " + d + " pasos.";\n    }\n    public static void main(String[] args) {\n        System.out.println(calcularDistanciaSalida(1, 1, 4, 4));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string CalcularDistanciaSalida(int fj, int cj, int fs, int cs) {\n        int d = Math.Abs(fj - fs) + Math.Abs(cj - cs);\n        return $"🧭 Distancia hasta la salida: {d} pasos.";\n    }\n    static void Main() {\n        Console.WriteLine(CalcularDistanciaSalida(1, 1, 4, 4));\n    }\n}`,
    cpp: `#include <iostream>\n#include <cmath>\nint calcularDistanciaSalida(int fj, int cj, int fs, int cs) {\n    return std::abs(fj - fs) + std::abs(cj - cs);\n}\nint main() {\n    std::cout << "Pasos: " << calcularDistanciaSalida(1, 1, 4, 4) << "\\n";\n    return 0;\n}`,
    javascript: `function calcularDistanciaSalida(fj, cj, fs, cs) {\n    const d = Math.abs(fj - fs) + Math.abs(cj - cs);\n    return \`🧭 Distancia hasta la salida 'X': \${d} pasos de movimiento.\`;\n}\nconsole.log(calcularDistanciaSalida(1, 1, 4, 4));\nconsole.log(calcularDistanciaSalida(0, 0, 3, 2));`,
    php: `<?php\nfunction calcularDistanciaSalida($fj, $cj, $fs, $cs) {\n    $d = abs($fj - $fs) + abs($cj - $cs);\n    return "🧭 Distancia hasta la salida: $d pasos.";\n}\necho calcularDistanciaSalida(1, 1, 4, 4) . "\\n";\n?>`,
    pseint: `SubProceso res <- calcularDistanciaManhattan(fj, cj, fs, cs)\n    d <- Abs(fj - fs) + Abs(cj - cs)\n    res <- "🧭 Distancia hasta la salida: " + ConvertirATexto(d) + " pasos."\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir calcularDistanciaManhattan(1, 1, 4, 4)\nFinAlgoritmo`
  },

  // 18. Encontrar el Cofre de Mayor Valor
  18: {
    python: `def encontrarCofreMasValioso(matrizTesoros):\n    maxOro = -1\n    mejorPos = (0, 0)\n    for r, fila in enumerate(matrizTesoros):\n        for c, valor in enumerate(fila):\n            if valor > maxOro:\n                maxOro = valor\n                mejorPos = (r, c)\n    return f"👑 Cofre más valioso en [{mejorPos[0]},{mejorPos[1]}] con {maxOro} monedas de oro."\n\ntesoros = [\n    [50,  200, 30],\n    [10,  800, 100],\n    [350, 20,  90]\n]\nprint(encontrarCofreMasValioso(tesoros))`,
    java: `public class Main {\n    public static String encontrarCofreMasValioso(int[][] mat) {\n        int max = -1, mr = 0, mc = 0;\n        for (int r = 0; r < mat.length; r++) {\n            for (int c = 0; c < mat[r].length; c++) {\n                if (mat[r][c] > max) { max = mat[r][c]; mr = r; mc = c; }\n            }\n        }\n        return "👑 Cofre más valioso en [" + mr + "," + mc + "] con " + max + " oro.";\n    }\n    public static void main(String[] args) {\n        int[][] t = {{50, 200}, {10, 800}};\n        System.out.println(encontrarCofreMasValioso(t));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string EncontrarCofreMasValioso(int[,] mat) {\n        int max = -1, mr = 0, mc = 0;\n        for (int r = 0; r < mat.GetLength(0); r++) {\n            for (int c = 0; c < mat.GetLength(1); c++) {\n                if (mat[r, c] > max) { max = mat[r, c]; mr = r; mc = c; }\n            }\n        }\n        return $"👑 Cofre en [{mr},{mc}] con {max} oro.";\n    }\n    static void Main() {\n        int[,] t = { {50, 200}, {10, 800} };\n        Console.WriteLine(EncontrarCofreMasValioso(t));\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\n#include <string>\nstd::string encontrarCofreMasValioso(const std::vector<std::vector<int>>& mat) {\n    int max = -1, mr = 0, mc = 0;\n    for (size_t r = 0; r < mat.size(); r++) {\n        for (size_t c = 0; c < mat[r].size(); c++) {\n            if (mat[r][c] > max) { max = mat[r][c]; mr = r; mc = c; }\n        }\n    }\n    return "👑 Cofre en [" + std::to_string(mr) + "," + std::to_string(mc) + "] (" + std::to_string(max) + " oro)";\n}\nint main() {\n    std::cout << encontrarCofreMasValioso({{50, 200}, {10, 800}}) << "\\n";\n    return 0;\n}`,
    javascript: `function encontrarCofreMasValioso(matrizTesoros) {\n    let maxOro = -1, mr = 0, mc = 0;\n    matrizTesoros.forEach((fila, r) => {\n        fila.forEach((val, c) => {\n            if (val > maxOro) { maxOro = val; mr = r; mc = c; }\n        });\n    });\n    return \`👑 Cofre más valioso en [\${mr},\${mc}] con \${maxOro} monedas de oro.\`;\n}\nconst t = [[50, 200, 30], [10, 800, 100], [350, 20, 90]];\nconsole.log(encontrarCofreMasValioso(t));`,
    php: `<?php\nfunction encontrarCofreMasValioso($mat) {\n    $max = -1; $mr = 0; $mc = 0;\n    foreach ($mat as $r => $fila) {\n        foreach ($fila as $c => $val) {\n            if ($val > $max) { $max = $val; $mr = $r; $mc = $c; }\n        }\n    }\n    return "👑 Cofre más valioso en [$mr,$mc] con $max oro.";\n}\n$t = [[50, 200], [10, 800]];\necho encontrarCofreMasValioso($t) . "\\n";\n?>`,
    pseint: `SubProceso res <- encontrarCofre(r, c, maxOro)\n    res <- "👑 Cofre más valioso en [" + ConvertirATexto(r) + "," + ConvertirATexto(c) + "] con " + ConvertirATexto(maxOro) + " oro."\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir encontrarCofre(1, 1, 800)\nFinAlgoritmo`
  },

  // 19. Dibujar Camino Marcado en la Mazmorra
  19: {
    python: `def dibujarCamino(mapa, startF, startC, pasos):\n    f, c = startF, startC\n    movs = {'U': (-1, 0), 'D': (1, 0), 'L': (0, -1), 'R': (0, 1)}\n    mapa[f][c] = "+"\n    for p in pasos:\n        if p in movs:\n            df, dc = movs[p]\n            f, c = f + df, c + dc\n            if 0 <= f < len(mapa) and 0 <= c < len(mapa[0]):\n                mapa[f][c] = "+"\n    return "👣 Ruta dibujada en el mapa con éxito."\n\nmapa = [[".", ".", "."], [".", ".", "."], [".", ".", "."]]\nprint(dibujarCamino(mapa, 0, 0, "DDRR"))\nfor fila in mapa: print(" ".join(fila))`,
    java: `public class Main {\n    public static void dibujarCamino(char[][] mapa, int f, int c, String pasos) {\n        mapa[f][c] = '+';\n        for (char p : pasos.toCharArray()) {\n            if (p == 'U') f--;\n            else if (p == 'D') f++;\n            else if (p == 'L') c--;\n            else if (p == 'R') c++;\n            if (f >= 0 && f < mapa.length && c >= 0 && c < mapa[0].length) mapa[f][c] = '+';\n        }\n    }\n    public static void main(String[] args) {\n        char[][] m = {{'.','.','.'}, {'.','.','.'}, {'.','.','.'}};\n        dibujarCamino(m, 0, 0, "DDRR");\n        for (char[] r : m) System.out.println(new String(r));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static void Main() {\n        Console.WriteLine("👣 Ruta trazada con éxito.");\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\n#include <string>\nvoid dibujarCamino(std::vector<std::vector<char>>& m, int f, int c, std::string p) {\n    m[f][c] = '+';\n    for (char ch : p) {\n        if (ch == 'U') f--; else if (ch == 'D') f++; else if (ch == 'L') c--; else if (ch == 'R') c++;\n        if (f >= 0 && f < (int)m.size() && c >= 0 && c < (int)m[0].size()) m[f][c] = '+';\n    }\n}\nint main() {\n    std::vector<std::vector<char>> m(3, std::vector<char>(3, '.'));\n    dibujarCamino(m, 0, 0, "DDRR");\n    std::cout << "👣 Camino marcado.\\n";\n    return 0;\n}`,
    javascript: `function dibujarCamino(mapa, startF, startC, pasos) {\n    let f = startF, c = startC;\n    const movs = { U: [-1, 0], D: [1, 0], L: [0, -1], R: [0, 1] };\n    mapa[f][c] = "+";\n    for (const p of pasos) {\n        if (movs[p]) {\n            const [df, dc] = movs[p];\n            f += df; c += dc;\n            if (f >= 0 && f < mapa.length && c >= 0 && c < mapa[0].length) mapa[f][c] = "+";\n        }\n    }\n    return "👣 Ruta dibujada en el mapa con éxito.";\n}\nconst mapa = [[".", ".", "."], [".", ".", "."], [".", ".", "."]];\nconsole.log(dibujarCamino(mapa, 0, 0, "DDRR"));\nmapa.forEach(r => console.log(r.join(" ")));`,
    php: `<?php\nfunction dibujarCamino(&$mapa, $f, $c, $pasos) {\n    $mapa[$f][$c] = "+";\n    foreach (str_split($pasos) as $p) {\n        if ($p === 'U') $f--; elseif ($p === 'D') $f++; elseif ($p === 'L') $c--; elseif ($p === 'R') $c++;\n        if (isset($mapa[$f][$c])) $mapa[$f][$c] = "+";\n    }\n}\n?>`,
    pseint: `SubProceso dibujarCamino(pasos)\n    Escribir "👣 Ruta '", pasos, "' trazada en el mapa con éxito."\nFinSubProceso\n\nAlgoritmo Principal\n    dibujarCamino("DDRR")\nFinAlgoritmo`
  },

  // 20. Verificar Victoria al Alcanzar la Salida ('X')
  20: {
    python: `def verificarVictoria(mapa, fJugador, cJugador):\n    if 0 <= fJugador < len(mapa) and 0 <= cJugador < len(mapa[0]):\n        if mapa[fJugador][cJugador] == "X":\n            return "🏆 ¡VICTORIA! Has llegado a la salida 'X'. ¡Mazmorra completada!"\n        else:\n            return f"🧭 Explorando celda [{fJugador},{cJugador}]. La búsqueda continúa..."\n    return "🚫 Posición fuera del calabozo."\n\nmapa = [\n    ["#", "#", "#"],\n    ["#", ".", "#"],\n    ["#", "X", "#"]\n]\nprint(verificarVictoria(mapa, 2, 1))\nprint(verificarVictoria(mapa, 1, 1))`,
    java: `public class Main {\n    public static String verificarVictoria(char[][] mapa, int fj, int cj) {\n        if (fj >= 0 && fj < mapa.length && cj >= 0 && cj < mapa[0].length) {\n            if (mapa[fj][cj] == 'X') return "🏆 ¡VICTORIA! Llegaste a la salida 'X'.";\n            else return "🧭 Explorando celda [" + fj + "," + cj + "].";\n        }\n        return "🚫 Fuera de la mazmorra.";\n    }\n    public static void main(String[] args) {\n        char[][] m = {{'#','#','#'}, {'#','.','#'}, {'#','X','#'}};\n        System.out.println(verificarVictoria(m, 2, 1));\n        System.out.println(verificarVictoria(m, 1, 1));\n    }\n}`,
    csharp: `using System;\nclass Program {\n    static string VerificarVictoria(char[,] mapa, int fj, int cj) {\n        if (fj >= 0 && fj < mapa.GetLength(0) && cj >= 0 && cj < mapa.GetLength(1)) {\n            if (mapa[fj, cj] == 'X') return "🏆 ¡VICTORIA! Llegaste a la salida 'X'.";\n            return $"🧭 Explorando celda [{fj},{cj}].";\n        }\n        return "🚫 Fuera de límites.";\n    }\n    static void Main() {\n        char[,] m = { {'#','#','#'}, {'#','.','#'}, {'#','X','#'} };\n        Console.WriteLine(VerificarVictoria(m, 2, 1));\n    }\n}`,
    cpp: `#include <iostream>\n#include <vector>\n#include <string>\nstd::string verificarVictoria(const std::vector<std::vector<char>>& m, int fj, int cj) {\n    if (fj >= 0 && fj < (int)m.size() && cj >= 0 && cj < (int)m[0].size()) {\n        return m[fj][cj] == 'X' ? "🏆 ¡VICTORIA! Salida alcanzada." : "🧭 Explorando...";\n    }\n    return "🚫 Fuera de limites.";\n}\nint main() {\n    std::cout << verificarVictoria({{'#','X'}}, 0, 1) << "\\n";\n    return 0;\n}`,
    javascript: `function verificarVictoria(mapa, fJugador, cJugador) {\n    if (fJugador >= 0 && fJugador < mapa.length && cJugador >= 0 && cJugador < mapa[0].length) {\n        if (mapa[fJugador][cJugador] === "X") {\n            return "🏆 ¡VICTORIA! Has llegado a la salida 'X'. ¡Mazmorra completada!";\n        }\n        return \`🧭 Explorando celda [\${fJugador},\${cJugador}]. La búsqueda continúa...\`;\n    }\n    return "🚫 Posición fuera del calabozo.";\n}\nconst mapa = [["#", "#", "#"], ["#", ".", "#"], ["#", "X", "#"]];\nconsole.log(verificarVictoria(mapa, 2, 1));\nconsole.log(verificarVictoria(mapa, 1, 1));`,
    php: `<?php\nfunction verificarVictoria($mapa, $fj, $cj) {\n    if (isset($mapa[$fj][$cj])) {\n        return $mapa[$fj][$cj] === "X" ? "🏆 ¡VICTORIA! Salida 'X' alcanzada." : "🧭 Explorando [$fj,$cj]...";\n    }\n    return "🚫 Fuera del calabozo.";\n}\n$mapa = [["#", "#", "#"], ["#", ".", "#"], ["#", "X", "#"]];\necho verificarVictoria($mapa, 2, 1) . "\\n";\n?>`,
    pseint: `SubProceso res <- verificarVictoria(enSalida)\n    Si enSalida Entonces\n        res <- "🏆 ¡VICTORIA! Has llegado a la salida 'X'."\n    Sino\n        res <- "🧭 Explorando la mazmorra..."\n    FinSi\nFinSubProceso\n\nAlgoritmo Principal\n    Escribir verificarVictoria(Verdadero)\nFinAlgoritmo`
  }
};
