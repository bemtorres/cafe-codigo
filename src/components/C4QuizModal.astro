---
import { c4QuizBank } from '../data/c4QuizBank';

type Props = {
  quizKey: string;
};

const { quizKey } = Astro.props as Props;
const quiz = c4QuizBank[quizKey];

if (!quiz) {
  throw new Error(`C4QuizModal: no existe quizKey "${quizKey}" en c4QuizBank.`);
}

const modalId = `c4-quiz-modal-${quizKey}`;
const openBtnId = `c4-quiz-open-${quizKey}`;
const closeBtnId = `c4-quiz-close-${quizKey}`;
const resultId = `c4-quiz-result-${quizKey}`;
const radiosNamePrefix = `c4_quiz_${quizKey}_q_`;
---

<div class="c4-quiz">
  <button id={openBtnId} type="button" class="c4-quiz__start" data-c4-quiz-open>
    Comenzar Test C4
  </button>

  <div id={modalId} class="c4-quiz__modal" role="dialog" aria-modal="true" aria-hidden="true">
    <div class="c4-quiz__overlay" data-c4-quiz-close></div>

    <div class="c4-quiz__panel">
      <div class="c4-quiz__header">
        <div class="c4-quiz__title">
          <span class="c4-quiz__title-badge">C4 Quiz</span>
          <span>{quiz.title}</span>
        </div>

        <button
          id={closeBtnId}
          type="button"
          class="c4-quiz__close"
          data-c4-quiz-close
          aria-label="Cerrar"
        >
          ✕
        </button>
      </div>

      <div class="c4-quiz__body">
        <p class="c4-quiz__intro">
          Valida tus conocimientos sobre el Modelo C4. Son <strong>{quiz.questions.length}</strong> preguntas.
        </p>

        <form class="c4-quiz__form" onsubmit="return false;">
          {quiz.questions.map((q, i) => (
            <section
              class="c4-quiz__q"
              data-c4-quiz-q={i}
              data-correct-index={q.correctIndex}
            >
              <h4 class="c4-quiz__q-title">
                <span class="c4-quiz__q-num">{i + 1}</span>. {q.prompt}
              </h4>

              <div class="c4-quiz__options">
                {q.options.map((opt, idx) => {
                  const name = `${radiosNamePrefix}${i}`;
                  const value = String(idx);
                  const id = `${radiosNamePrefix}${i}_${idx}`;
                  return (
                    <label class="c4-quiz__opt" for={id}>
                      <input
                        id={id}
                        type="radio"
                        name={name}
                        value={value}
                        data-c4-quiz-answer={i}
                      />
                      <span>{opt}</span>
                    </label>
                  );
                })}
              </div>

              <div
                class="c4-quiz__feedback"
                data-c4-quiz-feedback={i}
                aria-live="polite"
              ></div>
            </section>
          ))}
        </form>
      </div>

      <div class="c4-quiz__footer">
        <div id={resultId} class="c4-quiz__result" aria-live="polite"></div>
        <div class="c4-quiz__actions">
          <button type="button" class="c4-quiz__submit" data-c4-quiz-submit>
            Finalizar y Revisar
          </button>
          <button type="button" class="c4-quiz__restart" data-c4-quiz-restart>
            Reiniciar
          </button>
        </div>
      </div>
    </div>
  </div>
</div>

<script is:inline>
  (function () {
    const WKEY = '__c4QuizModalDelegated__';
    if (window[WKEY]) return;
    window[WKEY] = true;

    function closeModal(modal) {
      if (!modal) return;
      modal.setAttribute('aria-hidden', 'true');
      modal.classList.remove('is-open');
      document.documentElement.style.overflow = '';
    }

    function openModal(modal) {
      if (!modal) return;
      const resultEl = modal.querySelector('.c4-quiz__result');
      if (resultEl) resultEl.textContent = '';
      modal.querySelectorAll('[data-c4-quiz-feedback]').forEach((el) => {
        el.textContent = '';
      });
      modal.setAttribute('aria-hidden', 'false');
      modal.classList.add('is-open');
      document.documentElement.style.overflow = 'hidden';
    }

    function shuffleQuestions(modal) {
      const form = modal.querySelector('.c4-quiz__form');
      const qEls = Array.from(modal.querySelectorAll('[data-c4-quiz-q]'));
      if (!form || qEls.length < 2) return;

      for (let i = qEls.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        const tmp = qEls[i];
        qEls[i] = qEls[j];
        qEls[j] = tmp;
      }

      qEls.forEach((el) => {
        form.appendChild(el);
      });

      qEls.forEach((qEl, idx) => {
        const numEl = qEl.querySelector('.c4-quiz__q-num');
        if (numEl) numEl.textContent = String(idx + 1);
      });
    }

    function gradeModal(modal) {
      const resultEl = modal.querySelector('.c4-quiz__result');
      if (!resultEl) return;

      const qEls = modal.querySelectorAll('[data-c4-quiz-q]');
      let score = 0;

      qEls.forEach((qEl) => {
        const correct = Number(qEl.getAttribute('data-correct-index'));
        const feedbackEl = qEl.querySelector('[data-c4-quiz-feedback]');
        const selected = qEl.querySelector('input[type="radio"]:checked');
        const idx = selected ? Number(selected.value) : null;

        const correctInput = qEl.querySelector(`input[type="radio"][value="${correct}"]`);
        const correctText = correctInput?.parentElement?.querySelector('span')?.textContent ?? '';
        const selectedText = selected?.parentElement?.querySelector('span')?.textContent ?? '';

        const isCorrect = idx === correct;
        if (isCorrect) score++;

        if (feedbackEl) {
          if (isCorrect) {
            feedbackEl.innerHTML = '<span style="color:#05c290;">Correcto ✅</span>';
          } else {
            feedbackEl.innerHTML =
              `<span style="color:#D81B60;">Incorrecto ❌. Correcta: "${correctText}".</span>` +
              (selectedText ? `<br>Tu respuesta: "${selectedText}".` : '');
          }
        }
      });

      const total = qEls.length;
      if (score === total) resultEl.innerHTML = `🌟 ¡Excelente! Obtuviste ${score}/${total} puntos.`;
      else if (score > total / 2) resultEl.innerHTML = `👍 ¡Bien hecho! Obtuviste ${score}/${total}.`;
      else resultEl.innerHTML = `📚 Puntaje: ${score}/${total}. Sigue repasando.`;
    }

    document.addEventListener('click', (e) => {
      const target = e.target;

      const startBtn = target instanceof Element ? target.closest('.c4-quiz__start') : null;
      if (startBtn) {
        e.preventDefault();
        const root = startBtn.closest('.c4-quiz');
        const modal = root?.querySelector('.c4-quiz__modal') || null;
        openModal(modal);
        if (modal) shuffleQuestions(modal);
        return;
      }

      const closeBtn = target instanceof Element ? target.closest('[data-c4-quiz-close]') : null;
      if (closeBtn) {
        const root = closeBtn.closest('.c4-quiz');
        const modal = root?.querySelector('.c4-quiz__modal') || null;
        closeModal(modal);
        return;
      }

      const submitBtn = target instanceof Element ? target.closest('[data-c4-quiz-submit]') : null;
      if (submitBtn) {
        const root = submitBtn.closest('.c4-quiz');
        const modal = root?.querySelector('.c4-quiz__modal') || null;
        if (modal) gradeModal(modal);
        return;
      }

      const restartBtn = target instanceof Element ? target.closest('[data-c4-quiz-restart]') : null;
      if (restartBtn) {
        const root = restartBtn.closest('.c4-quiz');
        const modal = root?.querySelector('.c4-quiz__modal') || null;
        if (!modal) return;
        const resultEl = modal.querySelector('.c4-quiz__result');
        if (resultEl) resultEl.textContent = '';
        modal.querySelectorAll('input[type="radio"]').forEach((r) => {
          r.checked = false;
        });
        modal.querySelectorAll('[data-c4-quiz-feedback]').forEach((el) => {
          el.textContent = '';
        });
        return;
      }
    });

    document.addEventListener('keydown', (e) => {
      if (e.key !== 'Escape') return;
      document.querySelectorAll('.c4-quiz__modal.is-open').forEach((m) => closeModal(m));
    });
  })();
</script>

<style>
  .c4-quiz {
    margin: 2.5rem 0 2rem;
  }

  /* Color C4 Course (#1a5ebd) */
  .c4-quiz__start {
    width: 100%;
    max-width: 420px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.6rem;
    padding: 0.9rem 1.1rem;
    border-radius: 18px;
    border: 3px solid var(--border);
    background: #1a5ebd; 
    color: white;
    font-family: 'Nunito', sans-serif;
    font-weight: 900;
    cursor: pointer;
    box-shadow: 5px 5px 0px var(--border);
    transition: transform 0.1s, box-shadow 0.1s;
    margin: 0 auto;
  }

  .c4-quiz__start:hover {
    transform: translateY(-2px);
    box-shadow: 7px 7px 0px var(--border);
  }
  
  .c4-quiz__start:active {
    transform: translateY(2px);
    box-shadow: 3px 3px 0px var(--border);
  }

  .c4-quiz__modal {
    position: fixed;
    inset: 0;
    z-index: 2000;
    display: none;
  }

  .c4-quiz__modal.is-open {
    display: block;
  }

  .c4-quiz__overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(4px);
  }

  .c4-quiz__panel {
    position: relative;
    max-width: 860px;
    margin: 6vh auto 0;
    background: white;
    border: 3px solid var(--border);
    border-radius: 22px;
    box-shadow: 0 28px 90px rgba(30, 18, 16, 0.25);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    max-height: 88vh;
  }

  .c4-quiz__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    padding: 1rem 1.25rem;
    border-bottom: 3px solid var(--border);
    background: #F1F5F9; 
  }

  .c4-quiz__title {
    display: flex;
    gap: 0.75rem;
    align-items: center;
    font-family: 'Nunito', sans-serif;
    font-weight: 900;
    font-size: 1.1rem;
    color: #1a5ebd;
  }

  .c4-quiz__title-badge {
    background: #CBD5E1;
    color: #1E293B;
    border: 2px solid var(--border);
    padding: 0.25rem 0.7rem;
    border-radius: 999px;
    font-size: 0.85rem;
    box-shadow: 2px 2px 0px var(--border);
  }

  .c4-quiz__close {
    width: 42px;
    height: 42px;
    border-radius: 999px;
    border: 2px solid var(--border);
    background: rgba(255, 255, 255, 0.9);
    cursor: pointer;
    font-size: 1.05rem;
    font-weight: 900;
  }

  .c4-quiz__body {
    padding: 1rem 1.25rem;
    flex: 1;
    min-height: 0;
    overflow: auto;
  }

  .c4-quiz__intro {
    margin: 0 0 1rem;
    color: var(--text-secondary);
    font-weight: 700;
    line-height: 1.5;
  }

  .c4-quiz__q {
    padding: 0.85rem 0.85rem 1rem;
    border: 2px dashed rgba(30, 18, 16, 0.15);
    border-radius: 16px;
    background: #F8FAFC;
    margin-bottom: 1rem;
  }

  .c4-quiz__q-title {
    margin: 0 0 0.75rem;
    font-weight: 900;
    color: var(--text-primary);
    font-family: 'Nunito', sans-serif;
  }

  .c4-quiz__options {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.55rem;
  }

  .c4-quiz__opt {
    display: flex;
    gap: 0.7rem;
    align-items: flex-start;
    cursor: pointer;
    font-weight: 750;
    color: var(--text-secondary);
    line-height: 1.4;
  }

  .c4-quiz__opt input {
    margin-top: 2px;
    width: 18px;
    height: 18px;
    accent-color: #1a5ebd;
  }

  .c4-quiz__footer {
    padding: 0.95rem 1.25rem;
    border-top: 3px solid var(--border);
    background: #FAFAFA;
  }

  .c4-quiz__result {
    min-height: 1.25rem;
    margin-bottom: 0.75rem;
    font-weight: 900;
    color: #1a5ebd;
    font-family: 'Nunito', sans-serif;
  }

  .c4-quiz__feedback {
    margin-top: 0.65rem;
    font-weight: 900;
    line-height: 1.4;
    font-family: 'Nunito', sans-serif;
  }

  .c4-quiz__actions {
    display: flex;
    gap: 0.8rem;
    justify-content: flex-end;
    flex-wrap: wrap;
  }

  .c4-quiz__submit {
    border: 3px solid var(--border);
    background: #1a5ebd;
    color: white;
    padding: 0.65rem 1rem;
    border-radius: 14px;
    font-weight: 900;
    font-family: 'Nunito', sans-serif;
    cursor: pointer;
    box-shadow: 4px 4px 0px var(--border);
  }

  .c4-quiz__restart {
    border: 3px solid var(--border);
    background: white;
    color: var(--text-primary);
    padding: 0.65rem 1rem;
    border-radius: 14px;
    font-weight: 900;
    font-family: 'Nunito', sans-serif;
    cursor: pointer;
    box-shadow: 4px 4px 0px var(--border);
  }

  @media (max-width: 650px) {
    .c4-quiz__panel {
      margin-top: 4vh;
      width: calc(100% - 1.2rem);
    }
    .c4-quiz__actions {
      justify-content: stretch;
    }
    .c4-quiz__submit,
    .c4-quiz__restart {
      width: 100%;
      justify-content: center;
    }
  }
</style>
